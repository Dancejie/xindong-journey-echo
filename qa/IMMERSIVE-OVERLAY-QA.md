# 沉浸式叠层交互 QA（2026-08-25）

## 验收范围

- 视口：390×720。
- 本地入口：`http://127.0.0.1:4184/?intro=1`。
- 主角：姜米（ENFP）。
- 浏览器流程：节目介绍 → 选择姜米 → 首个过场 → 逐段旁白 → 三项选择 → 沈墨 1 对 1 → DeepSeek 实际回复。

## 结果

- 全屏过场首次播放结束后自动关闭并进入旁白；过场中仅保留声音开关和“跳过”，不存在“继续剧情”阻塞按钮。
- 过场播放期间旁白打字机保持在第 0 字；15.000 秒 R4 视频自然结束后旁白才从第一个字开始逐字出现，正文不会在遮罩后提前读完。
- 同一视频进入正文后继续作为静音循环背景，页面显示“循环已静音”，可手动重新开声。
- 场景媒体覆盖完整 390×720 视口；页面无横向溢出。
- 旁白阶段 `choices=0`。最后一次点击后进入退场阶段；旁白 DOM 与互动 DOM 不同时存在。互动阶段 `narration=0`、`choices=3`。
- 旁白、规则和选项均为深色半透明玻璃层；背景视频仍可辨认，无纯白正文板。
- 1 对 1 人物媒体覆盖完整聊天页；关闭、声音开关和输入框均在短屏可达范围内。
- 聊天记录区高 407px；实际写入一轮 DeepSeek 对话后 `atBottom=true`，玩家与嘉宾消息为左右半透明气泡。
- 当前视角姜米在人物栏中禁用，无法与自己私聊。
- 浏览器控制台 warning/error 为 0。

## 自动结束实测

- 首播过场：`loop=false`、`muted=false`、自然播放，无人工点击。
- 自然结束后：过场 dialog 数量为 0、旁白层数量为 1、选项数量为 0。
- 背景阶段：同一素材显示“循环已静音”；只承担动态氛围，不阻塞正文推进。

## 构建验证

- `python -m unittest backend.test_game_content backend.test_story_director`：54 项通过。
- `tsc --noEmit`：通过。
- `vite build`：通过。
- `git diff --check`：通过。
